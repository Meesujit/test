const add = (a: number, b: number): number => {
  return a + b;
};

console.log("Sum:", add(3, 4));

const sub = (a: number, b: number): number => {
  return a - b;
};

console.log("difference:", sub(10, 22));